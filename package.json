{
  "name": "ag-grid-poc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mock-server",
    "web"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev -w mock-server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w mock-server && npm run build -w web",
    "clean": "rm -rf node_modules mock-server/dist mock-server/node_modules web/dist web/node_modules"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.2"
  }
}
